<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE propertylist SYSTEM "http://www.sigames.com/dtds/sios/properties.dtd">

<!-- image properties -->
<properties>

	<!-- image borders -->
	<!-- these determine how many pixels any content is inset from each side of the image -->
	<!-- (for example, in the case of a button, the content would be the text label) -->
	<record id="image_borders" left="10" right="10" top="30" bottom="10"/>

	<!-- image slices determine the points at which the image will be sliced to enable scaling -->
	<!-- the image will only slice in the directions slicing values are provided for-->
	<record id="image_slices" left="2" right="2" top="2" bottom="2"/>

	<!-- title insets -->
	<!-- these determine how many pixels the title is inset from the top/bottom/left/right of the box -->
	<integer id="text_top_inset" value="5"/>
	<integer id="text_bottom_inset" value="0"/>
	<integer id="text_left_inset" value="0"/>
	<integer id="text_right_inset" value="0"/>

	<!-- title properties -->
	<record id="font_spec">
		<flags id="font" value="title"/> 
		<colour id="colr" name="heffem"/> 
		<flags id="size" value="14"/> 
	</record>

	<colour id="red_replacement" name="heffem3"/>
	<colour id="green_replacement" name=""/>
	<colour id="blue_replacement" name=""/>

	<boolean id="blurred" value="false"/>
	
</properties>
